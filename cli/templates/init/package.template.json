{
	"name": "{{PROJECT_NAME}}",
	"private": true,
	"type": "module",
	"scripts": {
		"collect": "harbor collect",
		"post-collect": "harbor post-collect",
		"build": "harbor build",
		"deploy:cloudflare": "harbor deploy cloudflare-pages",
		"dev": "harbor dev",
		"preview": "harbor preview"
	},
	"dependencies": {
		"agent-skill-harbor": ">={{PACKAGE_VERSION}} <1"
	}
}
